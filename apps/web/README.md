# Lab Orders Web

React/Vite SPA workspace. Run normal tasks from the repository root; see the root [`README.md`](../../README.md).

Vite listens on `WEB_PORT` (default `5173`, set in the root `.env`) and proxies `/api` to the Hono API on `API_PORT`.
